<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smrtsols.www.hall">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.Login">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Hall"/>


        <activity android:name=".Activities.Add_NewHall" />
        <activity android:name=".Activities.ViewHall" />
        <activity android:name=".Activities.ReservedHall" />
        <activity android:name=".Activities.CancelReservation" />
        <activity android:name=".Activities.Reservation"></activity>
    </application>

</manifest>